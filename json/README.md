# JSON

## Input parameter validation `reporting_additional_information`

- tested via `nf-schema` and a validation scheme (`assets/schema_flat_json.json`)
  - valid input should be one unnested json object